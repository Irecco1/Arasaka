-- require necessary files
local login_screen = require("ui.login_screen")
local main = require("main")
local settings = require("config.settings")
local hash = require("hash")



-- global variables used here for later:
LOGIN = "" --the current user login to be used for requests
SERVER_ID = 14745 --server id. If you want to change it, change it also on server software
CODE = 0

-- get key from .key if exists
CODE = hash.readKey()

-- get login from settings if it exists
local data = settings.get()
LOGIN = data.login

-- setup the palette colors
term.setPaletteColour(colors.red, 0xFF0000)

-- get modem peripheral
peripheral.find("modem", rednet.open)


login_screen()
-- after login_screen we should have LOGIN="nick" and CODE=numbers
-- we are gona use them in the future to send and recive messages
main()

